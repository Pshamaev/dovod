description = "DOVOD: вернуть реальные имена и адреса, собрать docx в Результат (аргумент - путь к папке дела, по умолчанию на два уровня выше папки для_модели)"
prompt = '''Ниже готовый результат работы dovod_restore.py. Ничего от себя не добавляй и не пересказывай, просто покажи это человеку как есть.

!{python "__DOVOD_TOOLS__\dovod_prompts.py" restore-run {{args}}}'''
